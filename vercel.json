{
  "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput",
  "devCommand": "python manage.py runserver",
  "framework": "django",
  "functions": {
    "api/wsgi.py": {
      "memory": 3008,
      "maxDuration": 60
    }
  },
  "outputDirectory": "staticfiles",
  "rewrites": [
    {
      "source": "/static/(.*)",
      "destination": "/static/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/api/wsgi.py"
    }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "mysite.settings",
    "DEBUG": "False",
    "PYTHONUNBUFFERED": "1"
  },
  "envVars": {
    "SECRET_KEY": {
      "description": "Generate a secure key at https://djecrety.ir/ or use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'"
    },
    "DATABASE_URL": {
      "description": "PostgreSQL connection string (optional, falls back to SQLite)"
    },
    "ALLOWED_HOSTS": {
      "description": "Comma-separated list of allowed hosts (e.g., yourapp.vercel.app)"
    }
  }
}
